(function($){
	//Init sKrollr
	var s = skrollr.init({
		render: function(data){
			//when debugging, this log the current scroll position to the console.
			//console.log(data.curTop);
		}
	});
})(jQuery);